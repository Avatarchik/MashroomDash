using UnityEngine;
using System.Collections;

public class GameManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //  CameraFade.StartAlphaFade (Color.black, true, 0.5f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
